// import produce from 'immer';

const INITIAL_STATE = {
  errorLogin: false,
};
export default function Login(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
