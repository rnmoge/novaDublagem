const TableGroupSize = {
  name: 'tableGroupSize',
  primaryKey: 'id',
  propertipes: {
    id: {type: 'int', indexed: true},
    group_size: 'string',
    description: 'string',
    update_at: 'string',
  },
};

export default TableGroupSize;
